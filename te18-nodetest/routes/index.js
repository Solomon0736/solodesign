var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', function(req, res, next) {
  res.render('index', { title: 'Express solomon' });
});

<div class="p-3 mb-2 bg-primary text-white">.bg-primary</div>
   
module.exports = router;
